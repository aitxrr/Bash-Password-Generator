# Bash-Password-Generator
A simple Bash script that generates random passwords. The user can choose the password length and decide whether to include uppercase letters, numbers, and special characters. Generated passwords are saved in a text file for later use.
